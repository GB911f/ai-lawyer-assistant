# Синтетические шаблоны

Бинарные DOCX-референсы генерируются командой:

```bash
uv run python scripts/generate_demo_assets.py
```

Шаблоны не происходят из документов заказчика и содержат только вымышленные реквизиты.
